using UnityEngine;
/*
public class EnemyStateManage : MonoBehaviour
{
    EnemyBaseState currentState;
    EnemyAttackState AttackState = new EnemyAttackState();
    EnemyChaseState ChaseState = new EnemyChaseState();
    EnemyRoamState RoamState =  new EnemyRoamState();

    void Start()
    {
        currentState = RoamState;

        currentState.EnterState(this);
    }

    void Update()
    {
        currentState.UpdateState(this);

    }

    void SwitchState(EnemyBaseState state)
    {
        currentState = state;
        state.EnterState(this);
    }

}
*/